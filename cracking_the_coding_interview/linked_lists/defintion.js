// Linked Lists
// - disadvantage: not constant lookup time, you have to go through every node
// - advantage: can add and remove items from the beginning of the list in constant time
// - Runner Technique: second pointer that iterates through the linked list twice as fast as the first pointer

